


const f = (data, cb) => {
    // do stuff with data
    if(someError)
        return cb(someError)
    return cb(null, response)
}


f(data,(err,data) =>{
    (er,data) => {
        // ...
    }
})


const x = () => new Promise((resolve, reject) => {
    // async work
    if(someError)
        return reject(someError)
    resolve(data)
})


// const y = ...

return x
.then(y)
.catch(err)
.then(z)
.catch(y)
.catch(err=>{/*log & return err resp*/})



class values {
    constructor(props) {
        super(props)
        this.value = null
    }

    getValue() {
        if(readdirSync.get(value)
            return Promise.resolve(this.value)
        
        retireveValueLogic()
        .then(data => this.value = data)
        .then(() => this.value)
    }
    

}

values.getValue().then(data => {/*...*/})

